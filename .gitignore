# Dependencies
node_modules/
**/node_modules/

# Build outputs
dist/
build/
out/
target/
.next/
.cache/
*.tsbuildinfo

# Exception: ship the compiled CLI bundle so users can install via
# `npm install -g github:OpenSubmissionn/Submission_Open` without a build step
!cli/dist/
!cli/dist/**

# Test / coverage
**/coverage/
test-ledger/
.anchor/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Lockfiles (keep root, ignore nested duplicates)
package-lock.json
**/coverage/
# External Claude skill submodules
.agents/skills/ext/
CLAUDE.local.md

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key
id_rsa*

# Claude Code & MCP (local-only)
.agents/
.claude
.mcp.json
.gitmodules
CLAUDE.md
.claude/
CLAUDE.local.md


# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

.agents


# Claude Code (local tooling, não vai pro repo)
.claude/
.github/workflows/claude-code.yml
.github/workflows/claude.yml

# Test outputs
sample-output.json

# Claude Code (local tooling, não vai pro repo)
.claude/
.github/workflows/claude-code.yml
.github/workflows/claude.yml

# Test outputs
sample-output.json

# Claude Code (local tooling, não vai pro repo)
.claude/
.github/workflows/claude-code.yml
.github/workflows/claude.yml

# Test outputs
sample-output.json

.playwright-mcp/ 